{
  "scripts": {
    "tailwind": "npx @tailwindcss/cli -i css/app.css -o dist/app.min.css --minify",
    "build": "npm run tailwind && bash scripts/cachebust.sh"
  },
  "devDependencies": {
    "tailwindcss": "^4.3.0"
  }
}
